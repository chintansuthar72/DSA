#include<bits/stdc++.h>
using namespace std;
    
int main(){
    /*
        get length of two LL.
        find diff between them.
        make two ptr.....ptr1 and ptr2.
        traverse big LL till diff counter.
        then traverse both pointer till NULL and check data and if ptr1==ptr2 then
        data of that pointer
    */
    return 0;
}